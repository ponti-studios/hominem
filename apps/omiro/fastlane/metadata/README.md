# App Store metadata

Store metadata and screenshots belong under `metadata/en-US/` and are uploaded
by the `submit_for_review` lane once the product copy and approved assets are
ready. The release workflow intentionally skips metadata until those files are
present, so it cannot silently publish invented or stale store copy.
