appId: com.pontistudios.hakumi.dev
---
# Navigates from the inbox to the Settings screen via the toolbar menu,
# then back to the inbox.
- runFlow: startup.yaml
- tapOn: Open inbox menu
- tapOn: Settings
- assertVisible: Settings
- tapOn:
    id: BackButton
- assertVisible:
    id: inbox-composer-input
