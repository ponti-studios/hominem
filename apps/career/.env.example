VITE_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/craftd_dev
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/craftd_dev
DATABASE_URL_TEST=postgresql://postgres:postgres@127.0.0.1:4433/app-test
VITE_PUBLIC_API_URL=http://localhost:3000

# Local S3-compatible storage for development. Start it with:
# docker compose -f apps/career/docker-compose.yml up -d minio minio-init
# No storage env vars are needed for local MinIO.
