{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["package.json", "pnpm-lock.yaml", ".env.local", "**/.env.*local"],
  "globalEnv": [
    "R2_ENDPOINT",
    "R2_ACCESS_KEY_ID",
    "R2_SECRET_ACCESS_KEY",
    "VITE_PUBLIC_API_URL",
    "WEB_URL",
    "API_URL",
    "NOTES_URL",
    "DATABASE_URL",
    "GITHUB_TOKEN",
    "EXPO_PUBLIC_API_BASE_URL",
    "OPENROUTER_API_KEY",
    "REDIS_URL",
    "RESEND_API_KEY"
  ],
  "concurrency": "8",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build/**", ".react-router/**", "app/routes/+types/**/*"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "dependsOn": ["^build"]
    },
    "db:migrate": {
      "dependsOn": ["^build"]
    },
    "db:rollback": {
      "dependsOn": ["^build"]
    },
    "db:status": {
      "dependsOn": ["^build"]
    },
    "format": {
      "inputs": ["src/**/*.{ts,tsx,js,jsx,json,md}", "app/**/*.{ts,tsx,js,jsx,json,md}"]
    },
    "lint": {
      "inputs": [
        "src/**/*.{ts,tsx,js,jsx,json,md,css}",
        "app/**/*.{ts,tsx,js,jsx,json,md,css}",
        "packages/platform/ui/tools/stylelint-config-void.cjs"
      ]
    },
    "start": {
      "dependsOn": ["build"]
    },
    "test": {
      "dependsOn": ["^build"],
      "inputs": [
        "src/**/*.{ts,tsx,js,jsx}",
        "app/**/*.{ts,tsx,js,jsx}",
        "test/**/*.{ts,tsx,js,jsx}"
      ]
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "inputs": ["**/*.ts", "**/*.tsx", "tsconfig.json"]
    }
  }
}
