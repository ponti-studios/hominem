/**
 * Mock user fixtures for local development
 * These users can be used to test different authentication scenarios
 */

import type { User } from './types';
export { MOCK_USERS, DEFAULT_MOCK_USER } from './auth.types';
export type { User };