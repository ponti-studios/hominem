# Atlas configuration for Hominem database
env:
  dev:
    url: postgresql://postgres:postgres@localhost:5432/hominem_dev?sslmode=disable
  prod:
    url: ${DATABASE_URL}

sql:
  schema:
    - file: schema.sql

lint:
  latest:
    ignore:
      - constraint_missing_enforcement
