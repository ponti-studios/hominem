# Journal

- 2026-04-18T06:24:23Z: Created work item `shared-composer-route-targeting-and-selection-chips`.
- 2026-04-19T00:00:00Z: Implemented `ComposerState` (`@Observable @MainActor final class`) singleton with per-target draft management (feed/chat/notes/hidden), note selection chips, @mention detection + note search, primary/secondary submit actions, and UserDefaults draft persistence. Created `SharedComposerCard` SwiftUI view with mention dropdown, selection chips, multi-line text field, disabled attachment/voice placeholders, and send/start-chat buttons. Wired into `RootView` via `.safeAreaInset(edge: .bottom)` on inbox and notes NavigationStacks with `onChange`-driven `updateTarget` calls. Removed embedded `composerBar` from `ChatScreen`; ChatScreen now reloads on `ComposerState.shared.messageSentCount` change. Added `sendingChatId` signal to ComposerState so ChatScreen's ThinkingRow reflects both local (regenerate/edit) and SharedComposerCard sends. BUILD SUCCEEDED.
