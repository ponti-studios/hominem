# Docker secrets
secrets/
*.secret
*.key

# Backup files
backups/*.sql
backups/*.dump

# Environment files with secrets (keep .env.example committed)
.env
.env.local
.env.production

# Temporary files
*.tmp
*.log
