---
applyTo: '**/*.{tsx,jsx}'
---

# React

Rules

- Global/UI state: Zustand.
- Server/local-first: React Query + IndexedDB.
- Forms: React Hook Form + Zod.
- Avoid inline JSX callbacks; memoize only after profiling.
- Use semantic HTML and accessible interactions.
