{
	"$schema": "https://railway.com/railway.schema.json",
	"build": {
		"builder": "NIXPACKS",
		"buildCommand": "yarn build"
	},
	"deploy": {
		"startCommand": "node apps/api/build/src/index",
		"healthcheckPath": "/status",
		"healthcheckTimeout": 100,
		"restartPolicyType": "NEVER"
	}
}
